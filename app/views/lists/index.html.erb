<div class="banner">
  <div class="container">
    <h1>Alx Ciné</h1>
  </div>
</div>
<div class="container my-3">
  <div class="container-header d-flex justify-content-between">
    <h3>My Lists</h3>
    <% # TODO: Change this button for a ERB Link %>
    <a href="<%= new_list_path %>" class="btn btn-success">Create a movie list</a>
  </div>
  <div class="list-container my-3">
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% @lists.each do |list|%>
        <div class="col">
          <div class="card">
            <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path list.photo.key, height: 300, width: 400, crop: :fill %>)">
              <%= link_to list.name, list_path(list), id:"card-title" %>
              <%= link_to "X", list_path(list), method: :delete, id:"delete-list", data: { confirm: "Are you sure?" }%>
            </div>
          </div>
        </div>
      </a>
    <% end %>
    <% # End of the card %>
  </div>
</div>
</div>
